<div class="navbar navbar-fixed-top breadcrumbs" role="navigation">
    <div class="container">
        <ol class="breadcrumb row">
            <?php foreach($links as $link) : ?>
                <li><?= $link; ?></li>
            <?php endforeach; ?>
        </ol>
    </div>
</div>
